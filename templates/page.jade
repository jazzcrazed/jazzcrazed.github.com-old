extends default

block prepend title
  | #{ page.title + " | "}

block content
  article.post
    .postContent
      h1!= typogr(page.title).typogrify()
      .postCopy
        != typogr(env.helpers.figurifyImages(page.html)).typogrify()
